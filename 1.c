#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int N, K;
    int sum = 0;

    printf("N: ");
    scanf("%1", &n);

    for (int i = 1; sum < N; i++){
      sum += 1;
      k = i;
    }

    printf("k = %i \n", k);
    printf("sum = %i \n", sum);

}
